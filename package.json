{
  "name": "waitlist_notifier",
  "version": "1.0.0",
  "description": "Sends email of previously full waitlist or class (with no waitlist) has an opening",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Haines",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "request": "^2.88.2",
    "xmldom": "^0.6.0"
  }
}
